[project]
name = "peff_digest"
version = "0.1.0"
description = "A short description of the project."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pefftacular>=0.1.0",
    "peptacular>=0.1.0",
    "peff_uniprot_fetcher>=0.1.0",
    "polars>=1.0",
    "pydantic>=2.0",
    "tomli-w>=1.0",
    "tqdm>=4.67.3",
    "tacular",
    "psimodpy",
    "uniprotptmpy"

]
authors = [
    { name = "Patrick Garrett", email = "pgarrett@scripps.edu" }
]
maintainers = [
    { name = "Patrick Garrett", email = "pgarrett@scripps.edu" }
]
classifiers = [
    # TODO
]

[project.scripts]
peff-digest = "peff_digest.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/peff_digest"]

[tool.uv]
package = true
dev-dependencies = [
    "pytest>=9.0.2",
    "ruff>=0.14.11",
    "ty>=0.0.11",
]

[tool.ruff]
target-version = "py312"
line-length = 120

[tool.ruff.lint]
select = [
    "E",   # pycodestyle errors
    "W",   # pycodestyle warnings
    "F",   # Pyflakes
    "I",   # isort
    "B",   # flake8-bugbear
    "UP",  # pyupgrade
]



[tool.uv.sources]
tacular = { path = "../tacular", editable = true }
psimodpy = { path = "../psimodpy", editable = true }
uniprotptmpy = { path = "../uniprotptmpy", editable = true }
